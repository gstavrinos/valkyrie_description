<robot xmlns:xacro="http://ros.org/wiki/xacro" name="imu">

	<xacro:macro name="imu_device" params="name DevicePort DeviceAPI">
		<Device id="${name}" type="imu">
			<DevicePort id="${DevicePort}" />
			<DeviceAPI id="${DeviceAPI}" />
		</Device>
	</xacro:macro>
</robot>